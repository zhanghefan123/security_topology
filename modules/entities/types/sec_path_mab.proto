syntax = "proto3";

option go_package = "../types";

enum SecPathMabStrategy {
  FIXED_BATCH = 0;
  DYNAMIC_BATCH = 1;
}

enum SecPathMabTopologyType {
  NON_LINEAR_TEST_TOPOLOGY = 0;
  LINEAR_TEST_TOPOLOGY = 1;
}